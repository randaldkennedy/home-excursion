document.addEventListener("DOMContentLoaded", () => {
  bindFilters();
  bindAreaFilter();
  bindTaskSort();
  bindTaskEditor();
  bindProjectDetails();
  bindExpenseEditor();
  bindExpenseFilters();
  bindExpenseNavigation();
  bindImageViewer();
  loadDashboard();
});
